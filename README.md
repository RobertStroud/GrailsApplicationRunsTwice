# GrailsApplicationRunsTwice
Demo project to show application.groovy being executed twice - if you type "grails run-app" rather than "./gradlew bootRun", the code in "application.groovy" is executed twice...
